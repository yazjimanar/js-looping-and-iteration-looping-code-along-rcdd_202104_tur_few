// Code your solutions in this file
const card = ["Ada", "Brendan", "Ali", "Lisa", "Kaitlin","Jan"];
function writeCards(card)
{
    for (let i = 0; i < card.length ; i++) {
        console.log(`Thank you ${writeCards[i]}, for the wonderful birthday gift!`);
          }
              return card;

}

writeCards(card);
